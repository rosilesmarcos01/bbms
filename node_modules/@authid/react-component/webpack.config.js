module.exports = {
    mode: 'production',
    entry: './example.jsx',
    output: {
        path: __dirname,
        filename: 'example.min.js',
    },
    module: {
        rules: [
            {
                test: /\.jsx$/,
                use: {
                    loader: 'babel-loader',
                    options: {
                        presets: [
                            '@babel/preset-env',
                            '@babel/preset-react',
                        ],
                    },
                },
            },
        ],
    },
};
