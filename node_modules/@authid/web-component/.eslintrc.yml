env:
  browser: true
extends: 'eslint:recommended'
parserOptions:
  ecmaVersion: 6
rules: {
  "complexity": ["error", 25],
  "no-unused-vars": ["error", {
    "argsIgnorePattern": "^_",
    "varsIgnorePattern": "^_"
  }],
}
globals:
  ENABLE_DEBUG: readonly
  BUILD_ID: readonly
